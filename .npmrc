# @react-three/drei declares peer react@^19 while this app uses React 18.
# Railway Railpack normally copies only package.json + lockfile for `npm ci`, so it would miss this file.
# package.json includes a no-op `preinstall` so Railpack includes the full app context (see Railpack Node docs).
legacy-peer-deps=true
