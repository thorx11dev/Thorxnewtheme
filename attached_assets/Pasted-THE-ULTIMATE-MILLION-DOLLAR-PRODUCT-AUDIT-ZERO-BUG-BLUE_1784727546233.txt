THE ULTIMATE "MILLION-DOLLAR PRODUCT" AUDIT & ZERO-BUG BLUEPRINT
We are not building a hobby project. Thorx is being positioned to scale as a world-class, high-end production platform. "Spec-compliant" is no longer the baseline—the new bar is absolute financial integrity, flawless user experience (UX), micro-frontend polish, and ironclad security.
You are ordered to halt all active feature development and perform a forensic, system-wide codebase audit. You must critically evaluate every single line of code, database query, API route, and UI component.
Generate a comprehensive, fully detailed audit report covering the following three categories. Do not summarize; I require absolute visibility into EVERY gap, vulnerability, or architectural flaw you discover.

CATEGORY 1: Mistakes, Bugs, and Gaps (Expectation vs. Reality)
Identify every feature that is half-baked, brittle, or diverges from expected user behavior:
The Points-Only Mandate: Where in the frontend (graphs, tables, dashboard widgets, history, notifications) does the raw PKR balance leak? The UI must strictly render TX-Points until the user reaches the final withdrawal screen.
Database Transactions & Integrity: Identify every data mutation (e.g., recordEarnEvent) that is not safely wrapped in a strict, rollback-capable database transaction.
Concurrency & Race Conditions: Audit all money-moving and state-mutating endpoints (e.g., processWithdrawal) for concurrency vulnerabilities, double-spend risks, and double-click flaws.
Precision & Floating-Point Drift: Locate any instance of native JavaScript float math (parseFloat, +, *) handling currency or points. These must be flagged for replacement with scaled integers (paisa) or precise decimal libraries.

CATEGORY 2: The "Million-Dollar Company" Standards Gap
Identify exactly what is required to elevate this codebase to an enterprise-grade, bulletproof system:
Security & Authentication: Audit all admin and team routes. Are there endpoints bypassing requireSessionAuth? Are critical routes (withdrawal execution, password resets, auth flows) missing rate-limiting (rate-limit)?
Payload Validation & Mass Assignment: Which routes directly spread req.body into database queries (mass-assignment vulnerability) or bypass strict Zod schema validation?
Database Performance & Indexing: Identify missing database indexes on high-frequency queries (e.g., users.performance_score, users.total_earnings) and point out where composite indexes are needed (e.g., on task_records).
Memory & Scale Bottlenecks: Where are we loading unbounded datasets into memory? (e.g., fetching the entire user table into memory for recomputeLeaderboardCache instead of aggregating via SQL).
Enterprise Observability & Infrastructure: Highlight the absence of automated test suites, structured JSON logging, distributed error tracking (e.g., Sentry), and robust database migration/rollback strategies.

CATEGORY 3: Ecosystem Disconnection & UX Friction
Analyze the connective tissue between features and the frontend execution across all devices:
Cross-Portal Data Synchronization: Identify state and data disconnects between the User Portal and the Team Portal. Are guild milestones, captain messages, and weekly targets perfectly synced end-to-end?
Responsive Design & Mobile Constraints: Scan the entire application (prioritizing the Team Portal) for scroll locks, overflowing tables, broken layouts, and anti-patterns in Tailwind CSS on mobile viewports.
Micro-Interactions & UX Polish: List every instance where loading states default to plain text ("...") instead of smooth skeleton loaders. Flag any mutations (copying links, sharing, saving settings) that succeed or fail silently without triggering a polished toast notification.

YOUR ACTION REQUIRED:
Do not write any temporary fixes or code patches yet. Your sole output right now must be the complete, unedited list of findings separated into these 3 categories.
At the end of your report, formulate a step-by-step master plan to execute these fixes. If you have any ambiguities regarding intended business logic or the flow of funds, list your questions explicitly so I can clarify them before we begin the remediation phase.



