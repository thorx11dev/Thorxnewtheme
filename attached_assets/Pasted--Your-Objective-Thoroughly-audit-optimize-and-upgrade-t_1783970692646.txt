**Your Objective:**
Thoroughly audit, optimize, and upgrade the Leaderboard system to ensure flawless functionality, 100% data accuracy, and strict alignment with high-end THORX standards.

**Known Bugs & Crucial Fixes (Address These First):**
I have already identified a specific list of bugs that need your immediate attention. Fix all of the following:

* **Backend (`server/storage.ts`):**
* `getReferralLeaderboard`: Missing `role='user'` and `isActive=true` filters on L1/L2 queries (it currently counts admin accounts).
* `getReferralLeaderboard`: `earningsFromUser` is hardcoded to `'0.00'`. You must use `commissionLogs.sourceUserId` to extract real values.
* `refreshLeaderboardCache`: L1 and L2 SQL subqueries are missing `is_active=true` AND `role='user'` filters.
* `getLeaderboardInsights`: `lastUpdated` returns a pre-refresh timestamp even after an auto-rebuild. It should return the current timestamp (`now`).
* `getLeaderboardInsights`: `availableBalance` is missing from the `globalRanking` select (which causes the CSV export to show 0 for every user).


* **Frontend (`LeaderboardInsights.tsx`):**
* `searchTerm` state is shared across all three tabs, causing typed text in one tab to bleed into the others. Isolate this state.
* `forceSyncMutation.onSuccess` invalidates a hardcoded key string, missing paginated pages. Update this to use a prefix match.
* The "Top Recruiters" tab has no sort capability; ensure columns are sortable.
* CSV export always shows 0 for "Available Balance" (ensure this works once the backend fix is applied).


* **Frontend (`UserPortal.tsx`):**
* "Referral LVL 2" metric card is hardcoded to 0. It must count `referralLeaderboard` entries where `level === 2`.
* "Referral LVL 1" metric card incorrectly shows `referralsData?.stats.count` (Total Referrals). It should exclusively show the L1 count.
* Clean up dead code: Remove `leaderboardData` (which contains fake names like "Don Ivan", "Saad Rauf", "Zain Abbas"), `sortedLeaderboard`, and `leaderboardTab`. They are unused and polluting the component.



**General Tasks & Execution Guidelines:**

* **Deep Comprehension First:** Before making any modifications, deeply analyze the codebase to fully understand the purpose, logic, and functionality of every single component within the Leaderboard.
* **100% Data Accuracy:** Audit the data fetching, mathematical calculations, and UI rendering logic. Ensure that every metric displayed across the Leaderboard components is completely accurate and reliably synced with the database.
* **Identify & Patch Additional Loopholes:** Beyond my list above, investigate the system for any other logical flaws, edge cases, or security vulnerabilities. If you find any hidden loopholes in how data is processed or displayed, fix them immediately.
* **Proactive Upgrades & New Features:** Evaluate the current state of the Leaderboard. If any feature feels basic, proactively upgrade it. Furthermore, **if you identify any missing features, advanced filters, or competitive metrics that a premium platform like THORX should have in this section, design and implement them from scratch.**
* **Maintain Premium Quality:** Ensure that all existing code and any brand-new features you introduce meet elite architectural, security, and performance standards.
* **Final Verification:** Ensure that after your updates, all components are working perfectly together without breaking any existing functionality.