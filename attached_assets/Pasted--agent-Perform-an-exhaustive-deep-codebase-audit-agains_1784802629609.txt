@agent Perform an exhaustive, deep-codebase audit against the updated system specification (THORX_SYSTEM_GUIDE.md) and our Million-Dollar Production Standard mandate. 

Your objective is twofold:
1. Deeply investigate and identify EVERY discrepancy, partial implementation, missing feature, or substandard code implementation across the entire codebase.
2. Automatically remediate and fix every issue identified to bring the Thorx platform to enterprise-grade, rock-solid quality.

---

### PHASE 1: COMPREHENSIVE INVESTIGATION & AUDIT

Execute a line-by-line inspection comparing the live codebase against `THORX_SYSTEM_GUIDE.md`. Specifically audit:

1. **TX-Point & Currency Integrity (Points-Only Mandate):**
   - Verify if any UI component or user-facing API response still leaks raw PKR/Cash values outside the final withdrawal preview card.
   - Check `balanceCashPkr` vs `txPointsBalance` usage. Ensure referral commissions and all user balances strictly follow the TX-Points primary representation with dynamic frontend conversion.
   - Confirm all financial math utilizes strict `Decimal` objects (no JS float arithmetic or native `toNumber()` precision losses mid-calculation).

2. **Earning Engines & Thorx Card Draw:**
   - Audit Engine A, B, C, and Indirect implementations. Ensure exact fee splits (Engine A/B: 40/60, Engine C: 20/35/45).
   - Validate `ILLUSION_VARIANCE_PCT` (±10% variance) and rank-based variance boosts (+5% A-Rank, +10% S-Rank) in the card draw math.

3. **Ranking & Performance Score (PS) Alignment:**
   - Verify Legacy Rank thresholds ($25k, 50k, 75k TX-Points) and PS-based Rank Tiers (E to S). Ensure double-condition checks (earnings + referrals) work without edge-case bugs.
   - Ensure the 48h inactivity penalty (-10 PS/day with 0 floor) is properly scheduled and functioning.

4. **Risk Engine & Background Performance:**
   - Audit Risk Engine queries. Confirm it uses an INCREMENTAL scan strategy (active users last 24-48h) on hourly runs, and full scans are offloaded to 6-hour windows.
   - Verify leaderboard refresh is optimized to a 15-minute interval with cache strategy.

5. **Security, Team Keys & Data Protection:**
   - Scan all user/team API payloads. Confirm `passwordHash` is 100% stripped from all responses.
   - Verify RBAC permissions (`MANAGE_USERS`, `MANAGE_PAYOUTS`, etc.) on routes like `/api/admin/users/:id/action`. Ensure endpoints are properly secured.
   - Confirm Idempotency controls (client UUID header + server Redis/DB lock) prevent duplicate withdrawal race conditions.

6. **Million-Dollar Code Quality Standards:**
   - Identify hardcoded magic numbers, missing database transactions during balance updates, unhandled promise rejections, lack of structured error handling, or missing audit logs for sensitive operations.

---

### PHASE 2: AUTOMATED REMEDIATION & REFACTORING

Once the investigation is complete, execute the fixes immediately:
- Refactor all non-compliant backend routes, database queries, and frontend UI components.
- Wrap all financial transactions in strict atomic DB transactions with rollback safety.
- Clean up any legacy/deprecated code (e.g., old commission logs, redundant floats, exposed debug keys).
- Run system checks/tests to ensure zero regressions across User, Guild, Team, and Admin portals.

Provide a concise breakdown of:
1. What was misaligned or broken.
2. What was missing or substandard.
3. The exact code fixes implemented to bring Thorx up to 100% compliance with `THORX_SYSTEM_GUIDE.md`.