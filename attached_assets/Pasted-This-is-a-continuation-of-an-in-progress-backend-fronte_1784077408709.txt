This is a continuation of an in-progress backend/frontend rebuild called "THORX v3" on the THORX rewards platform. Before writing any code, do a deep investigation to independently verify what has actually been done — do not trust any summary at face value, including this one.

Step 1 — Orient yourself:

Read .agents/memory/thorx-v3-rebuild.md in full — it's the durable log of decisions, gotchas, and status for this rebuild.
Read the full spec at attached_assets/task_1784072117657.md (2,701 lines) — this is the source of truth for every phase, table, endpoint, and invariant. Pay special attention to "Appendix A" (10 immutable invariants) — nothing you build may violate them.
Use explore subagents to independently re-verify Phase 1 (schema + server/modules/thorx-card.ts, ps-engine.ts, gps-engine.ts, live-feed.ts, server/middleware/rankGate.ts) and Phase 2 (recordEarnEvent, createWithdrawal, processWithdrawal, rejectWithdrawal, calculateWithdrawalBreakdown in server/storage.ts) against the spec text directly, line by line — do not assume prior work is correct just because it exists.
Step 2 — Confirm Phase 2 is genuinely solid:

Run npx tsc --noEmit -p . and confirm zero errors.
Restart the Start application workflow and confirm clean startup with no errors.
Re-run an end-to-end curl test against https://$REPLIT_DEV_DOMAIN (never localhost — see memory for why): register a test user + a referred test user, generate ad-view earnings, request a withdrawal, approve it as admin, and verify in the database that user_transactions rows are marked withdrawn, users.txPointsBalance/totalWithdrawn are correct, and referral_commissions (not commission_logs) received the referrer payout. Clean up all test data and any temporarily-lowered config (e.g. MIN_PAYOUT) afterward.
Step 3 — Continue the rebuild, starting with Phase 3: Per the 6-phase spec plan: Phase 3 is remaining routes (withdrawal preview endpoint, referral cash withdrawal, cron jobs, admin ledger validator, live feed SSE, Thorx Card simulator) and the health engine fix. Phases 4-5 are frontend (rank-tier terminology rename E/D/C/B/A/S-Rank, rankAvatars.ts updates, a real ThorxCard.tsx component, and — importantly — replacing the "Available Balance" PKR widget, which is now stale/frozen because v3 earn events no longer touch availableBalance; see memory for the exact reasoning). Phase 6 is cleanup (remove the rally system, old guild-vault-resolution.ts job, L2 referral code).

Standards to hold yourself to:

Treat this as production financial code. Every PKR/points calculation must trace back to spec text you've personally read, not inference.
No mocked or placeholder data, no silent fallbacks — if something is ambiguous or the spec conflicts with existing code (as happened with the withdrawal request/approval split in Phase 2 — see memory), stop and flag it explicitly rather than guessing.
After each phase, typecheck, restart the workflow, and curl-verify against the real dev domain before considering it done.
Update .agents/memory/thorx-v3-rebuild.md with any new non-obvious decisions or gotchas you hit, so the next agent doesn't have to rediscover them.
Aim for 100% fidelity to the spec's data model and invariants — this is a rewrite users' real money math depends on, not a rough approximation.