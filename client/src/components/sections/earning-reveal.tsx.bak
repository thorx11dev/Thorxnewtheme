import { Play, Users, CheckCircle2 } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import TechnicalLabel from "@/components/ui/technical-label";
import Barcode from "@/components/ui/barcode";

interface EarningRevealProps {
    isActive: boolean;
    onAdvance: () => void;
}

export default function EarningReveal({ isActive, onAdvance }: EarningRevealProps) {
    return (
        <section
            className={`cinematic-section ${isActive ? 'active' : ''} bg-[#EAE5DD]`}
            data-section="2"
            data-testid="earning-reveal-section"
        >
            <div className="w-full max-w-7xl mx-auto px-6 md:px-12 flex flex-col justify-start pt-20 md:pt-32 pb-12">
                <div className="mb-12 md:mb-16 text-center">
                    <div className="inline-block mb-4">
                        <TechnicalLabel text="METHODS TO EARN PKR" />
                    </div>
                    <h2 className="text-5xl md:text-7xl font-black tracking-tighter mb-4 text-foreground">How to Earn?</h2>
                    <p className="text-xl md:text-2xl text-[#FF6B00] font-bold max-w-2xl mx-auto">
                        Turn Your Attention into Real PKR
                    </p>
                </div>

                <div className="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-6xl mx-auto w-full">
                    {/* Watch Video Ads Card */}
                    <div className="split-card bg-white text-black p-8 md:p-12 text-center border-2 border-black relative overflow-hidden flex flex-col items-center">
                        <div className="absolute top-0 right-0 p-4 opacity-10 pointer-events-none">
                            <Play className="w-32 h-32" />
                        </div>

                        <div className="w-full mb-8">
                            <TechnicalLabel text="METHOD // 01" className="text-black/50" />
                        </div>

                        <h3 className="text-4xl md:text-5xl font-black tracking-tighter mb-4 uppercase text-black leading-tight">
                            WATCH VIDEO ADS
                        </h3>
                        <p className="text-[#FF6B00] text-lg font-bold italic tracking-tight mb-10">
                            Earn PKR by giving real attention
                        </p>

                        <div className="w-full space-y-4 mb-12 text-left bg-black/5 p-6 border-l-4 border-[#FF6B00]">
                            {[
                                "Watch short video advertisements inside the THORX player",
                                "Open the advertiser's page through THORX Web Panel",
                                "Read, scroll, and stay attentive for about 30 seconds",
                                "Your task converts into PKR only after real interaction"
                            ].map((item, i) => (
                                <div key={i} className="flex items-start gap-3">
                                    <CheckCircle2 className="w-5 h-5 text-[#FF6B00] mt-0.5 shrink-0" />
                                    <p className="text-black/80 text-base font-bold leading-tight">
                                        {item}
                                    </p>
                                </div>
                            ))}
                        </div>

                        <div className="mt-auto w-full">
                            <Barcode className="w-full h-12 bg-black/10 opacity-50 mb-2" />
                            <p className="text-[10px] font-mono font-black tracking-[0.2em] text-black/30 uppercase">
                                HONEST_EARNS // AD_REVENUE_VERIFIED
                            </p>
                        </div>
                    </div>

                    {/* Refer Friends Card */}
                    <div className="split-card bg-black text-white p-8 md:p-12 text-center border-2 border-white/10 relative overflow-hidden flex flex-col items-center">
                        <div className="absolute top-0 right-0 p-4 opacity-10 pointer-events-none">
                            <Users className="w-32 h-32 text-white" />
                        </div>

                        <div className="w-full mb-8">
                            <TechnicalLabel text="METHOD // 02" className="text-white/50" />
                        </div>

                        <h3 className="text-4xl md:text-5xl font-black tracking-tighter mb-4 uppercase text-white leading-tight">
                            REFER FRIENDS
                        </h3>
                        <p className="text-[#FF6B00] text-lg font-bold italic tracking-tight mb-10">
                            Build a team. Earn together.
                        </p>

                        <div className="w-full space-y-4 mb-12 text-left bg-white/5 p-6 border-l-4 border-[#FF6B00]">
                            {[
                                "Invite friends using your referral link or code",
                                "Earn commissions from your team's earnings",
                                "Multi-level referral system for long-term income",
                                "Commissions are credited when your team withdraws"
                            ].map((item, i) => (
                                <div key={i} className="flex items-start gap-3">
                                    <CheckCircle2 className="w-5 h-5 text-[#FF6B00] mt-0.5 shrink-0" />
                                    <p className="text-white/80 text-base font-bold leading-tight">
                                        {item}
                                    </p>
                                </div>
                            ))}
                        </div>

                        <div className="mt-auto w-full">
                            <Barcode className="w-full h-12 bg-white/10 opacity-50 mb-2" />
                            <p className="text-[10px] font-mono font-black tracking-[0.2em] text-white/30 uppercase">
                                NETWORK_SYNC // TEAM_VERIFY_ACTIVE
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    );
}
