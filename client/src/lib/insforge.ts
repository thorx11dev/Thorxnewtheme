/** Insforge removed — stubs kept for import compatibility during transition. */
export const insforge = {} as any;
export function isInsforgeConfigured(): boolean { return false; }
export async function hydrateInsforgeFromRefreshToken(): Promise<boolean> { return false; }
